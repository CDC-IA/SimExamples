para executar os códigos, rodar no terminal:

- pip install numpy scipy coppeliasim_zmqremoteapi_client pyzmq cbor

Para gravar os dados:
- python recordData.py 

Para plotar a fft
- python signalFFT.py